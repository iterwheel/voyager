[voyager]
work_dir = "~/.voyager/state"

[[apps]]
slug = "iterwheel-blueprint"
app_id = 1234567
private_key_path = "~/.secrets/iterwheel-blueprint.pem"
installation_id = "55544433"

[profiles.bad]
model = "deepseek-v4-pro"
thinking = true
# 0.0 effectively disables the confidence gate — must be > 0.0 to bound the
# investigator's quality floor.
min_confidence = 0.0
