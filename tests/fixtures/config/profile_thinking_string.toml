[voyager]
work_dir = "~/.voyager/state"

[[apps]]
slug = "iterwheel-blueprint"
app_id = 1234567
private_key_path = "~/.secrets/iterwheel-blueprint.pem"
installation_id = "55544433"

[profiles.bad]
model = "deepseek-v4-pro"
# Stringified bool — Python's bool("false") is True, which would silently flip
# the operator's intent. _parse_profile must reject non-bool TOML values.
thinking = "false"
