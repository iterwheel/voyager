[voyager]
work_dir = "~/.voyager/state"

[[apps]]
slug = "iterwheel-blueprint"
app_id = 1234567
private_key_path = "~/.secrets/iterwheel-blueprint.pem"
installation_id = "55544433"

[profiles.pro_max_effort]
model = "deepseek-v4-pro"
thinking = true
# DeepSeek V4 supports "max" for maximum-effort thinking (Codex PR #10 P2:
# the round-2 allowlist was {"low", "medium", "high"} which incorrectly
# rejected this operator-valid value).
reasoning_effort = "max"
